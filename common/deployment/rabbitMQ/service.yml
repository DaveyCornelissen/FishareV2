apiVersion: v1
kind: Service
metadata:
    name: "kubefish"
    labels:
        app: "kubefish"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "kubefish"